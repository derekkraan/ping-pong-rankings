- extra_column = Game.newer_than(5.minutes.ago).present?
%table.table.table-striped.table-bordered.table-condensed
  %thead
    %tr
      %th/
      %th Score
      %th/
      - if extra_column
        %th
  %tbody
    - games = Game.order('created_at DESC').limit(15)
    - games.each do |game|
      %tr{ :class => game.created_at > 5.minutes.ago ? 'success' : '' }
        - teams = game.teams
        %td.span2{ :style => 'text-align: right' }
          = teams.first.players.map(&:name).to_sentence
          = results_badge(teams.first)
        %td.span1{ :style => 'text-align: center' }
          = "#{teams.first.score}-#{teams.second.score}"
        %td.span2
          = results_badge(teams.second)
          = teams.second.players.map(&:name).to_sentence
        - if extra_column
          %td
            - if game.created_at > 5.minutes.ago
              = link_to game_path(game), :method => 'delete' do
                %span.icon-remove
