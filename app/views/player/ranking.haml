- content_for :title, 'Player Rankings'
.row
  .span6
    %table.table.table-striped
      - players = Player.order('score desc, name asc')
      %thead
        %tr
          %th #
          %th Name
          %th GP
          %th Rating
          %th Streak
      %tbody
        - last_score = false
        - rank = 1
        - players.each do |player|
          %tr
            %td= rank if player.score != last_score
            - rank += 1 if player.score != last_score
            - last_score = player.score
            %td
              - if player.winning_streak > 2
                %span.label.label-important ON FIRE
              - if player.losing_streak > 1
                %span.label LOSING
              = player.name
            %td= player.games.count
            %td= player.score
            %td= player.streak
